const CANVAS_WIDTH = 400;
const CANVAS_HEIGHT = 400;
const SCALE_FALLBACK = 1;
const SIZE_FALLBACK = 0;
const RECT_OPTIONS = {
  fill: 'transparent',
  stroke: 'red',
  strokeWidth: 3,
  selectable: false,
  originX: 'left',
  originY: 'top',
};
const TEXT_OPTIONS = {
  fontSize: 14,
  originX: 'center',
  originY: 'center',
  fontFamily: 'Tahoma',
  fontWeight: 'bold',
  fill: 'black',
  textBackgroundColor: 'white',
};

export {
  CANVAS_WIDTH,
  CANVAS_HEIGHT,
  SCALE_FALLBACK,
  SIZE_FALLBACK,
  RECT_OPTIONS,
  TEXT_OPTIONS,
}